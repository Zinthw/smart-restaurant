PORT=4000
DATABASE_URL=postgres://user:password@localhost:5432/smart_restaurant
QR_JWT_SECRET=super-secret-key-change-me
CLIENT_BASE_URL=http://localhost:5173
JWT_SECRET=khoa_bi_mat_dang_nhap_admin_123456

# Cấu hình gửi mail (Ví dụ dùng Gmail)
# Nếu dùng Gmail, mật khẩu phải là "App Password" (không phải pass đăng nhập google)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Link frontend để người dùng bấm vào reset (Ví dụ)
CLIENT_URL=http://localhost:5173

# Cấu hình OAuth Google (Frontend sẽ không gửi được token thật nếu không có ID thật)
GOOGLE_CLIENT_ID=your-google-client-id-from-console.apps.googleusercontent.com